<!-- Blog Sidebar Widgets Column -->

  <!-- Blog Search Well -->
  <div class="well">
    <h4>搜尋</h4>
    <div class="input-group">
      <input type="text" class="form-control">
      <span class="input-group-btn">
        <button class="btn btn-default" type="button">
          <span class="glyphicon glyphicon-search"></span>
        </button>
      </span>
    </div>
    <!-- /.input-group -->
  </div>

  <!-- Blog Categories Well -->
  <div class="well">
    <h4>文章分類</h4>
    <div class="row">
      <div class="col-lg-6">
        <ul class="list-unstyled">
          <% @categories.each do |category| %>
            <li><%= link_to "#{category[:name]} (#{category.posts.count})", "/categories/#{category[:name]}" %></a></li>
          <% end %>
        </ul>
      </div>
    </div>
    <!-- /.row -->
  </div>

  <div class="well">
    <h5><%= link_to "瀏覽所有文章", posts_overview_path %></h5>
  </div>

