<!-- Navigation -->
<nav class="custom-nav navbar navbar-default" role="navigation">
<div class="container">
  <!-- Brand and toggle get grouped for better mobile display -->
  <!-- <div class="navbar&#45;header"> -->
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="/"><%= image_tag("black_logo.png",class: "site-logo") %></a>
    <a class="navbar-brand site-title" href="/">特快車</a>
  <!-- </div> -->
  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <!-- <ul class="nav navbar&#45;nav"> -->
    <!-- </ul> -->
    <ul class="nav navbar-nav">
      <li><%= link_to("部落格", posts_path) %></li>
      <li><%= link_to("影片教學", trainings_path) %></li>
      <li><%= link_to("關於作者", author_path) %></li>
      <% if user_signed_in? %>
        <li>
        <% if is_admin? %>
          <%= link_to("後台", user_path(current_user["name"])) %>
        <% else %>
          <%= link_to("帳號資訊", user_path(current_user["name"])) %>
        <% end %>
        </li>
        <li>
        <%= link_to("登出", sign_out_path) %>
        </li>
      <% else %>
        <li>
        <% link_to('登入', sign_in_path) %>
        </li>
      <% end %>
    </ul>
  </div>
  <!-- /.navbar-collapse -->
</div>
<!-- /.container -->
</nav>

